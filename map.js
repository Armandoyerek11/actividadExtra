// el map te permite iterar sobre un arreglo y modificar sus elementos

// caso de uso: tenemos un array que nos suma la misma cantidad entre si 

const array1 = [1, 4, 9, 16];

const map1 = array1.map(x => x * 2);

console.log(map1);